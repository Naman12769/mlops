def create_feature(data):
  return data
# in notebooks data is in interactive manner and all of the data in single file in notebook like model info,datasets info and many more.
# but in src files code in not appropriate manner because all of the required data exist on same file it is not necessary. let's have a example. if i want to a datasets information in feature folder then we want to export datasets information from data folder.